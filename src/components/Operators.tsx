import Button from "./Button";

const Operators = () => {
  return (
    <div className="flex gap-2">
      <Button>/</Button>
      <Button>х</Button>
      <Button>-</Button>
      <Button>+</Button>
    </div>
  );
};

export default Operators;
